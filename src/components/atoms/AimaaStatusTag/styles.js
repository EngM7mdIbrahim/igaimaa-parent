import { StyleSheet } from "react-native";

export default StyleSheet.create({
  container: {
    minWidth: 80,
    alignContent:'center',
    justifyContent: 'center', 
    borderRadius: 20,
  },
});