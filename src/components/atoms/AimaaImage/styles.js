import { StyleSheet } from "react-native";
import { PRIMARY } from "_styles/colors";
import { border } from "_styles/mixins";

export default StyleSheet.create({
  container: {
    height: undefined,
    aspectRatio: 1
  },
  rounded: {
    borderRadius: 1000
  }
});